import {Button, Row} from 'react-bootstrap';
import {animated} from 'react-spring';

// animated bootstrap components
export const AnimatedButton = animated(Button);
export const AnimatedRow = animated(Row);